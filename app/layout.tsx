import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'AI Code Builder - Build Websites with Natural Language',
  description: 'AI-powered code generator with live preview. Build complete websites using natural language.',
  keywords: 'AI, code generator, web development, AI coding assistant, website builder',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
